{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: only application commands; no filesystem, shell or network plugin access.",
  "windows": ["main"],
  "permissions": []
}
