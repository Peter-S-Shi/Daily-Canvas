{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: only application commands, plus local notifications for Time Block reminders; no filesystem, shell or network plugin access.",
  "windows": ["main"],
  "permissions": ["notification:default"]
}
